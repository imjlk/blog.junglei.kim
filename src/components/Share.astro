---
import { siteConfig } from '@/data/site.config'
import TwitterIcon from './icons/TwitterIcon.astro'
import LinkedinIcon from './icons/linkedinIcon.astro'
const message = siteConfig.shareMessage

const URL = Astro.url.href
---

<div class='flex flex-col gap-2'>
	<span class='mb-1 font-bold text-2xl'>공유하기</span>
	<ul class='flex gap-3 text-black dark:text-white'>
		<li>
			<a
				href={`https://twitter.com/intent/tweet?text=${message + ' ' + URL}`}
				aria-label='X(Twitter)에 공유하기'><TwitterIcon /></a
			>
		</li>
		<li>
			<a
				href={`https://www.linkedin.com/shareArticle?mini=true&url=${URL}`}
				aria-label='LinkedIn에 공유하기'
			>
				<LinkedinIcon /></a
			>
		</li>
	</ul>
</div>
